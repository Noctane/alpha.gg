generator client {
  provider = "prisma-client-js"
  previewFeatures = ["referentialIntegrity"]
}

datasource db {
  provider = "mysql"
  url = env("DATABASE_URL")
  referentialIntegrity = "prisma"
}

// model Users {
//   id              Int      @id @default(autoincrement())
//   wordleScore     Int
//   sutomScore      Int
//   worldleGames    Int
//   sutomGames      Int
//   worldleAverage  Float
//   sutomAverage    Float
// }

model DailyScore {
  id          Int   @default(autoincrement()) @id
  userId      Int
  worldle     Int?
  sutom       Int?
}

